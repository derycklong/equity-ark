// Kept as a compatibility export for chart and feature modules while the
// application moves to the unified theme provider in hooks/useTheme.ts.
export { AppThemeProvider as ThemeProvider, useTheme } from "../hooks/useTheme";
