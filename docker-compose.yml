services:
  equity-ark:
    image: derycklong/equity-ark:2026.6.14
    ports:
      - "1340:80"
    volumes:
      - ./data:/app/data
      - ./backend/data:/app/backend/data
    env_file:
      - ./data/.env
    restart: unless-stopped
